###  Gitlab

```ruby
# https://stepik.org/learn
```

:white_check_mark: _<a name='#'> Настройка конфигураций Git</a>

```ruby
git config --global --list
git config --gl.aЬal user.name "<name>"
git config --gl.aЬal. user. email. "<email>"
```
